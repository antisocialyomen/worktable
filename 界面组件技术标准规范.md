# 个人工作台 · 界面组件技术标准规范

> 适用范围：单文件 HTML/JS 应用 `index.html`（Cloudflare Pages 部署）。
> 目的：统一桌面 / iOS / Android 三端所有按钮、文字、控件、框的尺寸、颜色、交互与断点，杜绝"每个功能各写各的像素、移动端按钮大小不一"的问题。
> 权威来源：一切以 `index.html` 中的 `:root` 令牌、`.icon-btn` / `.btn*` / `.in` / `.card` 等类定义为准；本文档为人工可读快照，代码改动后需同步更新本文件。

---

## 0. 强制原则（任何新增 / 修改功能必须遵守）

1. **移动端尺寸一律引用令牌，禁止手写 px。** 所有 ≤768px 下的按钮、输入框、图标按钮宽高，必须 `var(--mb-*)` 引用，不得写死 `width:34px` 之类字面量。
2. **按钮一律用标准类。** 方形图标按钮用 `.icon-btn`（含 `.icon-btn.danger`），文字按钮用 `.btn` / `.btn-p` / `.btn-a` / `.btn-o` / `.btn-sm`，禁止新建 `width:xx;height:xx` 的按钮样式。
3. **移动端规则就近写。** 某模块的移动端覆盖规则，写在该模块页面内联 `<style>` 的 `@media(max-width:768px)` 块内，便于同模块维护（见旅箱 `#p-tripbox` 做法）。
4. **跨平台一致性重置已全局生效**，新增控件无需重复添加 `appearance:none` / `box-sizing`，但也不要覆盖它们。
5. **断点唯一**：桌面 `≥769px`，移动 `≤768px`。不得引入其他断点。

---

## 1. 设计令牌（Design Tokens）

### 1.1 全局颜色 `:root`（index.html:21）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--primary` | `#9B7EDE` | 主色 / 品牌紫（主按钮、激活态、链接高亮） |
| `--primary-light` | `#B39BE8` | 主色浅（渐变终点、hover） |
| `--primary-dark` | `#8264CE` | 主色深（hover 加深） |
| `--primary-bg` | `#EFE9FB` | 主色浅底（hover 背景、图标浅底） |
| `--accent` | `#5FB8A8` | 强调青绿（次按钮 `.btn-a`、成功态） |
| `--accent-light` | `#E3F4F0` | 强调浅底 |
| `--alert` | `#FFC349` | 警示黄（提示、warning） |
| `--bg` | `#F3EEFC` | 页面背景（亮） |
| `--glass` | `rgba(248,244,255,.55)` | 毛玻璃卡片底（亮） |
| `--text` | `#2E2440` | 主文字 |
| `--text2` | `#5E5276` | 次文字（说明、标签） |
| `--text3` | `#9389A8` | 弱文字（占位、禁用、时间戳） |
| `--border` | `rgba(155,126,222,0.16)` | 边框 / 分隔线 |
| `--shadow` | `0 2px 8px rgba(155,126,222,0.12)` | 轻阴影 |
| `--shadow-lg` | `0 8px 25px rgba(155,126,222,0.18)` | 重阴影（卡片 hover、弹窗） |
| `--s` | `#4CAF85` | 状态·成功绿 |
| `--w` | `#FFC349` | 状态·警告黄 |
| `--d` | `#FF8383` | 状态·危险红（删除、错误） |
| `--font` | `-apple-system,BlinkMacSystemFont,'Segoe UI','PingFang SC','Microsoft YaHei',sans-serif` | 字体栈（优先系统字体，iOS/Android 一致） |

> 暗色主题由 `[data-theme=dark]` 覆盖同名令牌（index.html:38），组件无需单独适配。

### 1.2 圆角 / 间距 / 布局令牌

| 令牌 | 值 | 用途 |
|---|---|---|
| `--radius` | `16px` | 卡片、大容器圆角（移动端压缩为 `12px`） |
| `--radius-sm` | `10px` | 输入框、小组件圆角（移动端 `8px`） |
| `--radius-lg` | `24px` | 按钮 Pill 形圆角（移动端 `16px`） |
| `--nav-w` | `170px` | 桌面侧栏宽 |
| `--h` | `56px` | 顶栏高（移动端 `48px`） |
| `--t` | `.3s` | 统一过渡时长 |

### 1.3 移动端专用令牌 `:root`（index.html:23-37）

新增功能移动端样式**必须引用**以下令牌，改一处全站同步：

| 令牌 | 值 | 用途 |
|---|---|---|
| `--mb-break` | `768px` | 断点（语义标识，规则仍写在 `@media(max-width:768px)`） |
| `--mb-icon-btn` | `32px` | 主图标按钮边长（减 / 加 / 编辑 / 删除 / 展开收起），比桌面更紧凑，仍≥手指触控下限 |
| `--mb-icon-sm` | `28px` | 次要导航箭头（日历翻页等），小于主图标按钮 |
| `--mb-icon-btn-radius` | `8px` | 图标按钮圆角 |
| `--mb-icon-font` | `13px` | 图标按钮内图标字号 |
| `--mb-btn-pad` | `6px 12px` | 文字按钮内边距（横向从 14px 收紧，省空间） |
| `--mb-btn-font` | `13px` | 文字按钮字号 |
| `--mb-btn-radius` | `var(--radius-lg)` | 文字按钮圆角 |
| `--mb-in-pad` | `8px 10px` | 输入框内边距（纵向从 9px 收紧） |
| `--mb-in-font` | `14px` | 输入框字号（可读性下限，不降） |
| `--mb-card-pad` | `12px` | 卡片内边距（从 14px 收紧） |
| `--mb-gap` | `6px` | 控件间距（从 8px 收紧，提高信息密度） |
| `--mb-line` | `1.5` | 行高 |
| `--mb-hbtn` | `32px` | 顶栏 / 头栏图标按钮边长 |

---

## 2. 按钮规范

### 2.1 文字按钮（`.btn` 系列）

基础 `.btn`（index.html:154）：`display:inline-flex; align-items:center; gap:6px; padding:8px 16px; border-radius:var(--radius-lg); font-size:14px; font-weight:500;`

| 类 | 外观 | 用途 | hover |
|---|---|---|---|
| `.btn-p` | 紫色渐变 `primary→primary-light`，白字，阴影 | **主操作**（提交、确认、生成） | 加深 `primary-dark` + 上移 2px |
| `.btn-a` | 青绿 `accent` 实底白字 | 强调次操作 | 加深 `#4A9A8C` |
| `.btn-o` | 透明底 + `border` 细边 + `text2` 字 | 次要 / 取消 / 中性 | 紫边紫字 + `primary-bg` 底 |
| `.btn-sm` | 同 `.btn` 但 `padding:5px 10px; font-size:12px` | 紧凑场景（卡片头、行内） | 同对应变体 |
| `.btn-sm.rec` | 红色脉冲动画 | 语音记账录音中态 | — |

**移动端**（index.html:356-357）：`.btn` / `.btn-sm` 一律改为 `padding:var(--mb-btn-pad); font-size:var(--mb-btn-font); border-radius:var(--mb-btn-radius)`，三端文字按钮尺寸统一。

### 2.2 方形图标按钮（强制用 `.icon-btn`）

`.icon-btn`（index.html:164-167）：
```
width:var(--mb-icon-btn); height:var(--mb-icon-btn);   /* 32px */
border-radius:var(--mb-icon-btn-radius);                /* 8px */
border:1px solid var(--border); background:var(--bg-card);
color:var(--text2); font-size:var(--mb-icon-font);       /* 13px */
display:inline-flex; align-items:center; justify-content:center;
transition:all var(--t); flex-shrink:0; appearance:none
```
- `.icon-btn:hover` → 紫边紫字
- `.icon-btn.danger` → 红字红边；`.icon-btn.danger:hover` → 红底白字

**用途**：所有方形容积按钮——数量 `−` / `＋`、编辑 `fa-edit`、删除 `fa-trash`、展开 / 收起、收起 `↑` 等。旅箱 `.tb-btn`、任务 / 记账 / 健康 / 笔记 / 选题 / 素材各模块移动端图标按钮均已收编到此标准（32×32）。

> 桌面端遗留 `.btn-icon`（32×32）仍保留给桌面特定位置，但移动端全部覆盖为 `.icon-btn` 的 32px 令牌尺寸，保证双端一致。

### 2.3 顶栏图标按钮 `.hbtn`（index.html:102）

`width:36px; height:36px; border-radius:8px`（桌面）→ 移动端 `32px`（`.hbtn{width:32px;height:32px;font-size:15px}`，index.html:352）。

### 2.4 选择型 Pill 按钮组（标签 / 切换 / 分类选择）

多个模块用「圆角胶囊」承载**多选、单选、分类筛选**交互，视觉语言应统一，不得各写各的像素。统一契约：

- **形态**：`border-radius:var(--radius-sm)`（或 `20px` 全圆角）；细边 `var(--border)` + `var(--bg-card)` 底；未选 `var(--text2)` 字。
- **选中态**：`linear-gradient(135deg,var(--primary),var(--primary-light))` 紫渐变 + 白字 + 主色阴影（与 `.btn-p` 同款），禁止用其他色当选中态。
- **字号**：桌面 `12–13px`，移动端 `9–11px`（引用 `--mb-*` 或统一 `var(--mb-btn-font)`）。
- **禁用手写 px 宽**：用 `flex:1 1 auto` / `min-width` 弹性，避免窄屏溢出。

| 类 | 用途 | 位置 | 当前规格 | 合规 |
|---|---|---|---|---|
| `.ft .fb` | 筛选标签（记账/财务子页切换） | index.html:247 | `padding:3px 7px; font-size:9px`（移动端） | ✅ 已引用 |
| `#p-tripbox .tb-act` | 旅箱活动/类型多选切换 | index.html:987 | 桌面 `5px 11px / 12px`；移动 `flex:1 1 auto` 均分 | ✅ 走统一契约 |
| `.eat-cat` | 吃啥·分类筛选胶囊 | index.html:608 | `5px 12px / 13px`、`border-radius:20px`、`flex:0 0 auto` | ⚠️ 移动端横滚保留（`flex:0 0 auto` 合理，但字号应随令牌） |
| `.food-tab` / `.life-tab` / `.sched-tab` | 各模块大 Tab 切换容器 | index.html:595 等 | `display:block` 容器，子项用 `.btn-sm btn-o` | ✅ 复用标准按钮 |
| `.w-btn` | 吃啥·辣度/份量等选项按钮 | index.html:596（移动端 index.html:505） | 桌面 `min-width:56px; padding:6px 10px; font-size:13px`；移动端已引用 `var(--mb-btn-font)`/`var(--mb-btn-pad)` | ✅ 已整改（移动端对齐 `--mb-btn-*` 文字按钮令牌） |

> **整改完成（2026-09-01）**：移动端 `.w-btn`（index.html:505）已改为引用 `var(--mb-btn-font)` / `var(--mb-btn-pad)`，删除固定 `width:40px` 魔数，三端文字按钮尺寸体系统一。桌面端保留 `min-width:56px` 紧凑语义，未强行复用 `.btn` 变体（避免影响现有布局）。

---

## 3. 文字规范

| 层级 | 字号（桌面） | 字号（移动） | 字重 / 色 | 典型载体 |
|---|---|---|---|---|
| 页面标题 | `18px` | `15px` | `600` / `--text` | `.header-left h2` |
| 卡片标题 | `15px` | `14px` | `600` | `.card-hd h3` |
| 正文 | `14px` | `14px` | `400` / `--text` | 列表项、段落 |
| 输入文字 | `14px` | `14px` | `400` | `.in` |
| 次文字 | `12–13px` | `10–13px` | `400` / `--text2` | 说明、标签 |
| 弱文字 | `10px` | `9–10px` | `400` / `--text3` | 时间戳、徽标数值 |
| 大数字 | `24px` | `20px` | `700` | `.si .num`、统计 |

- **字体栈**：统一 `--font`（系统字体优先，iOS PingFang / Android Roboto 均走系统，避免自定义字体加载差异）。
- **行高**：正文 `1.6`（桌面 body），移动端控件区 `1.5`（`--mb-line`）。
- **数字**：金额、计数等用 `font-variant-numeric:tabular-nums` 等宽数字，对齐不抖。
- **抗锯齿**：`body{-webkit-font-smoothing:antialiased}`（index.html:66），防止 Android 字体发虚。
- **防缩放**：`body{-webkit-text-size-adjust:100%; text-size-adjust:100%}`（index.html:66），防止 iOS 横竖屏 / Android 文字自动放大。

---

## 4. 输入控件规范

> 所有输入控件（text/number/textarea/select/date 按钮）均享毛玻璃底 `var(--bg-card)`、细边 `var(--border)`、`--radius-sm` 圆角，并被 §8 跨平台重置（appearance:none）抹平 iOS/Android 差异。

### 4.1 文本输入框 `.in`（index.html:207）

适用于 `<input type="text">` / `number` / `textarea` 等文本类输入。

```
.in{flex:1; padding:8px 14px; border:1px solid var(--border);
    border-radius:var(--radius-sm); background:var(--bg-card);
    color:var(--text); font-size:14px}
.in:focus{border-color:var(--primary); box-shadow:0 0 0 3px rgba(155,126,222,0.15)}
```
- **聚焦**：紫边 `var(--primary)` + 紫光晕 `0 0 0 3px rgba(155,126,222,.15)`（index.html:209）。
- **移动端**（index.html:358）：`padding:var(--mb-in-pad); font-size:var(--mb-in-font)`（**9px 11px / 14px**），三端文本框字号统一为 14px。
- 文本输入框**不做固定宽**，由 `.ig` 弹性分配或整行 `flex:1`。

### 4.2 下拉框 `select.in`（index.html:210）

适用于所有 `<select>` 下拉选择。**与文本框分离定义**——下拉需自定义箭头、且移动端字号更小（箭头挤占宽度）。

```
select.in{
  appearance:none;
  background-image:url("...自定义 SVG 箭头...");     /* 右侧 6px 向下箭头 */
  background-repeat:no-repeat; background-position:right 6px center;
  padding-right:16px;                                /* 给箭头留位 */
}
```
- **桌面**：字号 `14px`、内边距同 `.in`（`8px 14px`，因 `padding-right:16px` 右侧略大）。
- **移动端**（index.html:361）：`font-size:11px!important; padding:2px 5px!important; height:auto` —— 下拉项比文本框小，避免窄屏挤占；箭头仍保留。
- **禁止**：下拉框写死 `width:60px` 类内联宽度（见 §10 反例）。移动端应改为弹性宽（`flex:0 1 auto; min-width:auto; width:auto`，index.html:362）。
- 自定义箭头 SVG 颜色 `#6B7280`，随主题切换需另配（当前亮色固定，暗色下略浅，已知待优化）。

### 4.3 输入组 `.ig`（index.html:174）

`display:flex; gap:8px; margin-bottom:12px`
- 组内 `.in` 桌面端 `flex:1`，可被内联 `flex:0 0 auto; width:xx` 覆盖（见 §10 反例注意）。
- **移动端**（index.html:359-362）：`flex-wrap:wrap; gap:3px`，组内 `.in` 改为 `flex:0 1 auto; min-width:auto; width:auto`，避免横排溢出。
- 组内按钮 `.ig .btn` 保持 `flex:0 0 auto`。

### 4.4 日期按钮 `.dt-btn` / `.dt-date-btn`（日期选择器触发按钮）

> 注意：这是**触发**日期选择器的按钮控件（不是弹窗本身）。自定义滚轮日期选择器（倒数日 / 日历等模块）用它；`<input type="date">`（旅箱、记账 `expDate`）是原生控件，样式走 §4.1。

- **`.dt-btn`**（index.html:454，移动端定义）：`display:inline-flex; align-items:center; gap:3px; padding:2px 6px; font-size:9px; line-height:1.2; border:1px solid var(--border); border-radius:var(--radius-sm); background:var(--bg-card); color:var(--text); cursor:pointer; white-space:nowrap; height:23px`；内部图标 `.dt-btn i{font-size:10px; color:var(--text2)}`。
  - 桌面端无独立 `.dt-btn` 定义，按通用 `.in` 风格呈现；移动端走上方 23px 高 / 9px 字规则。
- **`.dt-date-btn`**（index.html:517，移动端覆盖）：`font-size:11px!important; padding:2px 5px!important; width:auto!important; height:auto!important`；`.dt-date-btn .dt-btn-text{font-size:9px}`。
- **标准**：日期按钮高度统一（移动端 23px / `min` 不锁死），文字 ≤11px 不换行，图标 10px；点击触发 `.dt-picker`（§4.5）或系统原生 `input[type=date]`。

### 4.5 日期选择器弹窗 `.dt-picker`

自定义滚轮面板（index.html:270-284）：`max-width:400px; background:var(--bg-card); border-radius:16px; padding:0`，滚轮列 `.dt-col-item` 行高 `36px`、选中态 `font-size:18px; font-weight:700; color:var(--primary)`。
- **移动端**（index.html:457-458）：底部弹出 `border-radius:16px 16px 0 0`，底部留 `env(safe-area-inset-bottom)` 安全区（刘海/Home 条适配）。
- 旅箱、记账的 `<input type="date">` 走系统原生选择器，无需此弹窗。

### 4.6 多行文本域 `.log-area`（textarea 专用）

适用于多行文本输入（心情日志正文、备注长文本等），**不与 `.in` 共用**——它带最小高度与可拖拽。

```
.log-area{                           /* index.html:646 */
  width:100%; min-height:96px; resize:vertical;
  padding:10px 12px; border:1px solid var(--border);
  border-radius:var(--radius-sm); background:var(--bg-card);
  color:var(--text); font-size:14px; line-height:1.6; font-family:inherit
}
.log-count{text-align:right; font-size:10px; color:var(--text3); margin-top:4px}  /* 字数统计 */
```

- 共享毛玻璃底 / 细边 / `--radius-sm`，与 `.in` 视觉一致。
- **聚焦**：✅ 已补 `.log-area:focus`（index.html:647），与 `.in:focus` 一致——`border-color:var(--primary);box-shadow:0 0 0 3px rgba(155,126,222,.15)`。
- **移动端**：跟随 `.in` 令牌（`padding:var(--mb-in-pad); font-size:var(--mb-in-font)`），行高 `1.5`（`--mb-line`）。

---

## 5. 卡片与容器规范

### 5.1 卡片 `.card`（index.html:107）

`background:var(--bg-card); border-radius:var(--radius); border:1px solid var(--border); padding:20px; margin-bottom:12px; backdrop-filter:blur(18px) saturate(160%)`
- 毛玻璃质感统一（亮 / 暗主题由 `--bg-card` / `[data-theme=dark]` 自动切换）。
- **移动端**：`padding:14px; margin-bottom:10px; border-radius:var(--radius)`（12px，index.html:353）。

### 5.2 卡片头 `.card-hd`（index.html:112）

`display:flex; align-items:center; justify-content:space-between; margin-bottom:16px`
- 标题 `h3`：`font-size:15px; font-weight:600`（移动端 `14px`）。
- 移动端允许 `flex-wrap:wrap; gap:3px`（标题与右侧按钮可换行，index.html:354）。

### 5.3 统计卡 `.sc`（index.html:128）

`background:var(--bg-card); border-radius:var(--radius); border:1px solid var(--border); padding:16px 20px; display:flex; align-items:center; gap:16px`
- hover：重阴影 + 上移 2px。

### 5.4 图标方块 `.si`（index.html:130）

`width:44px; height:44px; border-radius:12px; font-size:20px`（桌面）→ 移动端 `40×40 / 18px`（index.html:368）。
- 配色变体：`.si1` 红、`.si2` 紫、`.si3` 黄、`.si4` 青绿（浅底深字）。

### 5.5 弹窗 `.modal`（index.html:637）

`background:var(--bg-card); border:1px solid var(--border); border-radius:var(--radius-lg); box-shadow:var(--shadow-lg); width:100%; max-width:440px; max-height:90vh; overflow-y:auto; padding:22px`
- 遮罩 `.modal-mask` 半透明，移动端居中。

### 5.6 媒体卡 `.mc`、统计明细 `.stat-*`

同享毛玻璃底（index.html:111），圆角 / 边框与 `.card` 一致。

### 5.7 开关 `.toggle`（index.html:230）

设置页 / 各项开关（深色模式、各类总开关）。

```
.toggle{width:44px; height:24px; background:var(--border); border-radius:12px; position:relative; cursor:pointer; flex-shrink:0; transition:background var(--t)}
.toggle::after{content:''; position:absolute; top:2px; left:2px; width:20px; height:20px; border-radius:50%; background:#fff; transition:all var(--t)}   /* 滑块 */
.toggle.on{background:var(--primary)} .toggle.on::after{transform:translateX(20px)}   /* 开态：主色 + 滑块右移 */
```

- **开态**用主色 `--primary`（与全站主色一致），不得用其他色。
- **移动端**（index.html:417-418）：`width:40px; height:22px`，滑块 `18×18`——双端尺寸接近，触控友好。

### 5.8 轻提示 `.toast`（index.html:228）

全局轻量反馈浮层（操作成功 / 失败提示）。

```
.toast{position:fixed; bottom:24px; left:50%; transform:translateX(-50%);
  background:var(--text); color:var(--bg); padding:10px 20px;
  border-radius:var(--radius-sm); font-size:14px; font-weight:500;
  z-index:10001; opacity:0; pointer-events:none; white-space:pre-wrap; transition:opacity .3s}
```

- 用**深色底 + 浅字**（与 `.modal` 毛玻璃底区分，强调"瞬时反馈"）。
- **移动端**（index.html:419）：`bottom:72px`（避开底栏 `m-nav` 高度）、`font-size:13px; padding:8px 16px; max-width:90%; white-space:normal; text-align:center`——避免长提示溢出屏宽。
- 显示用 `toast('...')` 函数控制 `opacity` 淡入淡出，非阻塞。

### 5.9 弹窗标题栏与关闭按钮 `.modal-hd` / `.modal-x`（index.html:638-640）

弹窗内部结构与右上角关闭按钮（§5.5 只写了 `.modal` 容器，这里补全内构件）。

```
.modal-hd{display:flex; align-items:center; justify-content:space-between; margin-bottom:16px}
.modal-x{cursor:pointer; color:var(--text3); font-size:20px; width:32px; height:32px;
  display:flex; align-items:center; justify-content:center; border-radius:8px}   /* 关闭按钮 32×32 */
.modal-x:hover{background:var(--bg)}
```

- `.modal-x` 是方形图标按钮，桌面 `32×32`、圆角 `8px`；**应纳入 `.icon-btn` 体系**（圆角用 `--mb-icon-btn-radius`，悬停紫底）。

### 5.10 遮罩 `.mask` / `.modal-mask`（浮层背景）

两类遮罩并存，注意区分：

- `.modal-mask`（index.html:268）：日期选择器遮罩，`rgba(0,0,0,.4)` + `z-index:10000`，移动端底部对齐（`.dt-picker-mask{align-items:flex-end}`，index.html:457）。
- `.mask`（index.html:635）：心情日志等弹层遮罩，`rgba(40,20,30,.45)` + `blur(3px)` 毛玻璃 + `z-index:10000`。
- **统一约定**：遮罩层一律 `position:fixed; inset:0; z-index:10000; display:flex; align-items:center; justify-content:center`，背景用半透明深色；新增弹层**复用 `.mask` 或 `.modal-mask`**，禁止新建第 N 个遮罩类。

### 5.11 日历控件 `.bill-cal-grid` / `.cal-day`（index.html:291-300）

记账 / 日程的月历网格（7 列）。

```
.bill-cal-grid{display:grid; grid-template-columns:repeat(7,1fr); gap:2px; text-align:center}
.cal-wk{font-size:10px; color:var(--text3); padding:4px 0; font-weight:600}        /* 星期表头 */
.cal-day{font-size:12px; padding:6px 0; border-radius:var(--radius-sm); cursor:pointer; color:var(--text2); transition:all .15s}
.cal-day.today{font-weight:700; color:var(--primary)}
.cal-day.selected{background:var(--primary); color:#fff; font-weight:600}          /* 选中：主色填充 */
.cal-day.other{color:var(--text3); opacity:.4}                                    /* 非本月 */
.cal-day.has-record::after{...4px 圆点 主色...}                                    /* 有记录标记 */
```

- 日期格**选中态用主色填充**（与全站选中态一致），禁止其他色。
- 日历头部的「上一月 / 下一月」圆按钮 `.bill-cal-hd button`（圆形，index.html:289）属于**次要导航箭头**，引用 `var(--mb-icon-sm)`（28px）/ `var(--mb-icon-font)`（13px），刻意小于主图标按钮 `var(--mb-icon-btn)`（32px）——翻页箭头触碰频率低，缩小更省空间，仍清晰可点。

### 5.12 颜色选择 `.cd-swatch`（index.html:301）

倒数日事件色板（圆形色点）。

```
.cd-swatch{width:26px; height:26px; border-radius:50%; cursor:pointer; flex:0 0 auto}
```

- 固定 `26×26` 圆点，选中态由父项 `.cd-item::before` 左侧色条（`width:4px`）表达，不缩放圆点本身。
- 三端一致（无移动端覆盖），符合「小色块不随屏缩放」原则。

### 5.13 心情选择 `.mood-cell`（index.html:643）

心情日志的 16 宫格表情选择器。

```
.mood-cell{aspect-ratio:1; display:flex; align-items:center; justify-content:center;
  font-size:26px; border-radius:12px; background:var(--bg-card);
  border:1px solid var(--border); cursor:pointer; transition:all var(--t)}
.mood-grid{display:grid; grid-template-columns:repeat(6,1fr); gap:8px; margin:8px 0 6px}
```

- 正方形用 `aspect-ratio:1` 自适应，**不写死宽高**，随容器列数变化；选中态应加主色边 / 紫底（当前由 JS 控制，建议统一 `.mood-cell.on` 类）。
- 图标 `26px` 为桌面基准，移动端可随网格列数自然缩小（不锁死，符合响应式）。

### 5.14 进度条 `.tb-prog` / 进度可视化

- **旅箱打包进度** `.tb-prog`（index.html:994）：`height:10px; border-radius:6px; background:var(--border); overflow:hidden`，内部 `>i` 渐变紫填充 `width:%`，`transition:width .3s`——**全站进度条统一用此形态**（细圆角槽 + 主色渐变填充 + 宽度过渡）。
- **首页点阵进度** `.dotbar` / `.pixel` / `.yb-*`（index.html:626-627）：游侠自创的点阵百分比可视化，属特殊样式，单独维护，不强制纳入通用进度条。

### 5.15 导航项 `.nav-item` / `.m-nav-item`

桌面侧栏（`.nav-item`，index.html:89）与移动底栏（`.m-nav-item`，index.html:236）的导航入口。

- `.nav-item`（桌面）：`display:flex; gap:8px; padding:7px 10px; margin:1px 6px; border-radius:var(--radius); font-size:13px; font-weight:500`；hover 紫底紫字（未写在 89 行但由 JS/通用 hover 处理）。
- `.m-nav-item`（移动）：`flex-direction:column; gap:2px; font-size:10px`，图标 `17px`（index.html:343）；`.nav-item` 移动端转竖排 `font-size:7px`、图标 `14px`（index.html:327-329）。
- **统一**：图标用 Font Awesome 同字号体系；选中态主色高亮；徽标 `.badge` 绝对定位右上角（见 §7.3）。

### 5.16 设置列表项 `.si-item` / `.si-l` / `.si-d`（index.html:253-256）

设置页的「标签 + 开关 / 说明」行。

```
.si-item{display:flex; align-items:center; justify-content:space-between; padding:14px 0; border-bottom:1px solid var(--border)}
.si-l{font-size:14px} .si-d{font-size:10px; color:var(--text3)}   /* 主文字 / 次说明 */
```

- 移动端 `.si-item{padding:10px 0}`、`.si-l/.si-d{font-size:10px}`（index.html:414-416）——字号收敛、不换行。
- 与 `.cb`（§6.1）同享底边分隔线语言，但 `.si-item` 用于「设置型行」（右置开关/控件），`.cb` 用于「勾选清单项」，职责分离。

---

## 6. 列表与清单项规范

### 6.1 清单项 `.cb`（index.html:216）

`display:flex; align-items:center; gap:10px; cursor:pointer; padding:8px 0; border-bottom:1px solid var(--border)`
- 勾选框 `.cc`：圆点 / 方块（移动端 `18×18`，index.html:374）。
- 文字 `.ct`：主文字（移动端 `font-size:10px`，index.html:375）。
- **移动端**：`.cb{padding:6px 0; flex-wrap:wrap}`（index.html:373），长文字可换行。
- 操作区 `.cb-acts`：桌面 / 移动均为图标按钮组，移动端对齐到 `.icon-btn` 34px 标准（旅箱已改用专属 `.tb-acts` + `.tb-btn`，不使用通用竖排）。

### 6.2 交易明细 `.txn-acts`、任务 `.task-acts`

图标按钮统一收编为 `--mb-icon-btn`（34px）令牌尺寸（index.html:378-380）。

---

## 7. 标签与徽标规范

### 7.1 标签 `.tag`（index.html:211）

`display:inline-flex; align-items:center; padding:2px 10px; border-radius:12px; font-size:10px; font-weight:500`
- 配色：`.t1` 青绿、`.t2` 紫、`.t3` 黄、`.t-urgent` 红（index.html:212-215）。
- 清单项内小标签（如"必需""自定义"）用 `padding:1px 6px; font-size:10px`（index.html:381）。

### 7.2 筛选标签 `.ft` / `.fb`（index.html:247）

`.ft{display:flex; gap:4px; flex-wrap:wrap}`；`.fb` 项 `padding:3px 7px; font-size:9px`（移动端）。选中态高亮。

### 7.3 侧栏徽标 `.badge`（index.html:93）

`background:var(--d); color:#fff; font-size:10px; padding:0 6px; border-radius:9px; min-width:16px`，移动端 `font-size:9px; min-width:12px`（index.html:330）。

---

## 8. 跨平台一致性重置（iOS / Android 抹平）

以下全局规则已生效（index.html:63-66），是双端视觉一致的根基：

1. `*,*::before,*::after{box-sizing:border-box}` —— 宽度计算统一（含 padding/border），避免 iOS/Android 盒模型差异。
2. `input,textarea,select,button{ -webkit-appearance:none; appearance:none; font-family:inherit; font-size:inherit; outline:none; border:none; background:none; -webkit-tap-highlight-color:transparent }` —— 去掉 iOS Safari 按钮自带圆角 / 内边距、Android 点按灰块。
3. `body{ -webkit-font-smoothing:antialiased; -webkit-text-size-adjust:100%; text-size-adjust:100% }` —— 防字体发虚、防系统自动缩放。
4. 安全区：`body{padding-top:env(safe-area-inset-top)}`、`.app{height:calc(100vh - env(safe-area-inset-top))}`、移动端 `height:100dvh`（index.html:40,82,321），适配刘海屏。

---

## 9. 响应式断点约定

| 区间 | 规则块 | 关键差异 |
|---|---|---|
| 桌面 `≥769px` | 默认样式 + `@media(min-width:769px)` | 侧栏 170px 展开、内联添加按钮显示、统计 4 列 |
| 移动 `≤768px` | `@media(max-width:768px)` | 侧栏收为 36px 图标条、内容 `padding:12px`、`--h:48px`、卡片 `14px`、按钮 / 输入引用 `--mb-*`、图标按钮 34px |

> 移动端侧栏隐藏为底部 `m-nav`（图标 + 9px 文字），点击菜单时侧栏以遮罩滑出；此布局由 `@media(max-width:768px)` 控制（index.html:322-345）。

---

## 10. 反例与正例

**✅ 正例（符合标准）**
```css
/* 旅箱移动端图标按钮，引用令牌 */
#p-tripbox .tb-btn{width:var(--mb-icon-btn);height:var(--mb-icon-btn);font-size:var(--mb-icon-font);border-radius:var(--mb-icon-btn-radius)}
/* 通用方形按钮 */
<button class="icon-btn danger"><i class="fas fa-trash"></i></button>
/* 下拉框移动端：弹性宽，不写死，箭头保留 */
#p-tripbox select.in, select.in{flex:0 1 auto;min-width:auto;width:auto;font-size:11px;padding:2px 5px}
/* 日期按钮触发日期选择器 */
<button class="dt-btn" data-dt-target="expDate"><i class="fas fa-calendar"></i><span class="dt-btn-text">2026-09-01</span></button>
```

**❌ 反例（禁止）**
```css
/* 手写像素，iOS/Android 与全站不一致 */
.tb-btn{width:28px;height:28px}
/* 新建按钮类而非复用 .btn* / .icon-btn */
.del-btn{width:30px;height:30px;border-radius:6px;background:#f00}
/* 移动端另写固定宽导致溢出 */
.ig select.in{flex:0 0 140px}
/* 文本/下拉框内联写死像素宽：记账 expType/expCat/expSub/expNl/expAmt 现状（index.html:826-839），
   移动端全部挤一行溢出——应改为弹性宽或整行堆叠 */
<input class="in" id="expCat" style="flex:0 0 auto;width:60px">
<select class="in" id="expSub" style="flex:0 0 auto;width:60px"></select>
/* ✅ 已修复：吃啥 .w-btn 移动端（index.html:505）已引用 --mb-btn-pad / --mb-btn-font，删除固定 width:40px */
.w-btn{font-size:var(--mb-btn-font)!important;padding:var(--mb-btn-pad)!important;min-width:auto!important;width:auto!important}
/* ✅ 已修复：日历头圆按钮（index.html:289）已改 var(--mb-icon-btn) / var(--mb-icon-font) */
.bill-cal-hd button{width:var(--mb-icon-btn);height:var(--mb-icon-btn);border-radius:50%;font-size:var(--mb-icon-font)}
/* ✅ 已修复：多行文本域已补 .log-area:focus（index.html:647），与 .in:focus 一致 */
.log-area:focus{border-color:var(--primary);box-shadow:0 0 0 3px rgba(155,126,222,.15)}

---

## 11. 维护清单（改动后同步更新）

- 新增 / 调整按钮尺寸 → 改 `:root` 的 `--mb-*` 或 `.btn*` / `.icon-btn`，不要散改各模块。
- 新增移动端适配 → 在该模块内联 `<style>` 的 `@media(max-width:768px)` 内写，且只引用令牌。
- 任何"按钮大小不一"反馈 → 先查是否漏用 `.icon-btn` 或手写 px，统一收编而非逐个修。
- 本规范与 `index.html` 冲突时，以代码为准，并回写本文件。

---
*文档生成：2026-09-01；修订①：2026-09-01 补齐第 4 节（下拉框 select.in §4.2、日期按钮 .dt-btn/.dt-date-btn §4.4，修正文本框移动端字号，记账表单内联宽列为反例）；修订②：2026-09-01 系统自查补齐其余遗漏控件——§2.4 选择型 Pill 按钮组（.tb-act/.w-btn/.eat-cat/.food-tab，标注 .w-btn 移动端未接令牌）、§4.6 多行文本域 .log-area、§5.7 开关 .toggle、§5.8 轻提示 .toast、§5.9 弹窗关闭 .modal-x、§5.10 遮罩 .mask/.modal-mask、§5.11 日历控件 .bill-cal-grid/.cal-day、§5.12 颜色选择 .cd-swatch、§5.13 心情选择 .mood-cell、§5.14 进度条 .tb-prog、§5.15 导航项 .nav-item/.m-nav-item、§5.16 设置列表项 .si-item；§10 反例新增 .w-btn/.bill-cal-hd button/.log-area:focus 三处不一致。对应代码：index.html:230(.toggle)、:228(.toast)、:638-640(.modal-x/.modal-hd)、:635(.mask)、:268(.modal-mask)、:291-300(.bill-cal-grid/.cal-day)、:289(.bill-cal-hd button)、:301(.cd-swatch)、:643(.mood-cell)、:994(.tb-prog)、:89(.nav-item)、:236(.m-nav-item)、:253(.si-item)、:646(.log-area)、:596(.w-btn)。*；**修订③（2026-09-01）**：上述三处代码债务已落地整改——`.w-btn` 移动端引用 `--mb-btn-*` 令牌（删固定 `width:40px`）、`.bill-cal-hd button` 改用 `var(--mb-icon-btn)`(34px)/`var(--mb-icon-font)`、`.log-area` 补 `:focus` 紫边紫光晕；§2.4/§4.6/§5.11 状态与 §10 反例同步更新为已整改。*；**修订④（2026-09-01）**：采纳用户反馈「移动端控件过于大、与桌面同尺寸浪费窄屏空间」，**收紧移动端令牌**——`--mb-icon-btn`/`--mb-hbtn` 34px→32px、`--mb-btn-pad` 8px14px→6px12px、`--mb-in-pad` 9px11px→8px10px、`--mb-card-pad` 14px→12px、`--mb-gap` 8px→6px，新增 `--mb-icon-sm`(28px) 专用于日历翻页等次要导航箭头（`.bill-cal-hd button` 改引用它）；§2.2/§2.3/§5.11 与令牌表同步更新。原则：紧凑但不破手指触控下限（主图标按钮 32px、次要箭头 28px、字号 13/14px 保可读性），靠收紧内边距/间距/卡片留白提信息密度，而非无脑缩小到不可点。*

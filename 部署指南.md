# 🎀 部署指南：Cloudflare Pages + 豆瓣 API 代理

## 准备工作

1. 注册 [Cloudflare](https://dash.cloudflare.com/) 账号（免费）
2. 准备好工作台文件：`index.html` + `_worker.js`

---

## 部署步骤

### 第一步：上传文件到 GitHub（或直接拖拽）

**方式 A：通过 GitHub（推荐）**
1. 创建 GitHub 仓库，把 `index.html` 和 `_worker.js` 推上去
2. 进入 Cloudflare Dashboard → Workers & Pages → 创建 Pages
3. 选择「连接到 Git」→ 授权 GitHub → 选择仓库
4. 构建配置：框架预设选「None」，构建命令留空，构建输出目录不填
5. 点击「保存并部署」

**方式 B：直接上传（更简单）**
1. 进入 Cloudflare Dashboard → Workers & Pages → 创建 Pages
2. 选择「直接上传」→ 创建项目名称
3. 把 `index.html` 和 `_worker.js` 拖拽上传
4. 点击「部署」

### 第二步：大功告成 🎉

部署成功后，你会得到一个 `https://xxx.pages.dev` 的链接：
- 手机电脑都能打开
- 新剧速递直接走豆瓣 API，有评分、海报、剧集信息
- 热点新闻实时抓取微博/抖音/知乎等平台
- 所有数据都在浏览器本地存储

---

## 验证豆瓣 API 是否生效

1. 打开部署后的网址
2. 进入「新剧速递」→ 点击「刷新」
3. 如果看到带评分（⭐ 8.5）和海报封面的卡片 → ✅ 成功！
4. 如果还是显示纯文字内容 → 说明 Worker 没生效，检查 `_worker.js` 是否在根目录

---

## 文件结构

```
个人工作台/
├── index.html      # 主页面（所有功能）
└── _worker.js      # Cloudflare Worker（豆瓣 API 代理）
```

> ⚠️ `_worker.js` 必须和 `index.html` 放在同一目录下，Cloudflare Pages 会自动识别并启用 Worker。

---

## 自定义域名（可选）

1. 在 Cloudflare Pages 项目设置中 → 自定义域
2. 输入你的域名 → 按照提示添加 DNS 记录
3. 等待 SSL 证书生效（通常几分钟）

---

## 本地测试

如果你想在本地测试 Worker 效果：

1. 安装 Node.js
2. 安装 `wrangler` CLI：`npm install -g wrangler`
3. 在项目目录运行：`wrangler pages dev .`
4. 打开 `http://localhost:8788` 测试

---

## 常见问题

**Q：部署后新剧速递还是刷不出数据？**
A：打开浏览器开发者工具（F12）→ 控制台，看看有没有报错。如果看到 `/_worker.js` 相关的 404 错误，说明文件位置不对，确保 `_worker.js` 在部署的根目录。

**Q：数据安全吗？**
A：所有数据存储在浏览器本地（localStorage），Worker 只做请求转发，不记录任何数据。

**Q：Cloudflare Pages 免费额度够用吗？**
A：完全够用。免费计划包含：
- 无限带宽
- 每月 10 万次 Worker 请求
- 全球 CDN 加速
- 个人工作台一天用不了几百次请求，绰绰有余 ✨
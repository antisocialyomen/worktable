# 🎀 部署指南：Cloudflare Pages + GitHub 自动部署 + 豆瓣 API 代理

## 架构
- 代码托管：GitHub 仓库 `antisocialyomen/worktable`（`https://github.com/antisocialyomen/worktable.git`）
- 自动部署：Cloudflare Pages 连接该仓库，**push 即部署**，无需手动上传
- 豆瓣代理：`_worker.js`（根目录，Cloudflare 自动识别并启用）

---

## 日常改动流程（最常用）

本地改完文件后，在项目目录执行：

```bash
git add -A
git commit -m "改动说明"
git push
```

Cloudflare 会在几分钟内自动重新部署。再也不用手动传整个目录。

---

## 首次部署（已完成）

1. Cloudflare Dashboard → **Workers & Pages** → 创建 → **Pages** → **连接到 Git**
2. 授权 GitHub，选择 `worktable` 仓库
3. 构建配置：框架预设 **None** / 构建命令 **留空** / 输出目录 **留空**
4. 部署完成得到 `https://xxx.pages.dev`

> ⚠️ Cloudflare Pages 的「直接上传」项目**不能**改成「连接 Git」。要换方式必须新建 Pages 项目选 Git，旧的直接上传项目可删，避免两个版本混淆。

---

## 文件结构（必须全部进仓库）

```
个人工作台/
├── index.html          # 主页面（所有功能 + 壁纸背景）
├── _worker.js          # Cloudflare Worker（豆瓣 API 代理）
├── assets/
│   ├── bg-wallpaper.jpg   # 页面壁纸
│   └── labubu-avatar.jpg  # 侧栏头像
├── .gitignore          # 排除 _verify.py / _verify.png / .workbuddy 等
└── 部署指南.md
```

> ⚠️ `_worker.js` 必须与 `index.html` 同目录，Cloudflare 才会启用豆瓣代理。
> ⚠️ `assets/` 必须包含，否则线上壁纸 / 头像 404 裂图。

---

## 验证豆瓣 API 是否生效

1. 打开部署后的网址
2. 进入「新剧速递」→ 点击「刷新」
3. 如果看到带评分（⭐ 8.5）和海报封面的卡片 → ✅ 成功！
4. 如果还是显示纯文字内容 → 说明 Worker 没生效，检查 `_worker.js` 是否在根目录

---

## 自定义域名（可选）

1. 在 Cloudflare Pages 项目设置中 → 自定义域
2. 输入你的域名 → 按照提示添加 DNS 记录
3. 等待 SSL 证书生效（通常几分钟）

---

## 本地测试

如果你想在本地测试 Worker 效果：

1. 安装 Node.js
2. 安装 `wrangler` CLI：`npm install -g wrangler`
3. 在项目目录运行：`wrangler pages dev .`
4. 打开 `http://localhost:8788` 测试

---

## 常见问题

**Q：改完页面线上没变？**
A：先确认本地执行了 `git push`；再去 Cloudflare 项目「部署」页看最新一次是否绿色成功。有时是浏览器缓存，按 **Ctrl+F5** 强刷。

**Q：部署后新剧速递还是刷不出数据？**
A：打开浏览器开发者工具（F12）→ 控制台，看看有没有报错。如果看到 `/_worker.js` 相关的 404 错误，说明文件位置不对，确保 `_worker.js` 在部署的根目录。

**Q：数据安全吗？**
A：所有数据存储在浏览器本地（localStorage），Worker 只做请求转发，不记录任何数据。

**Q：Cloudflare Pages 免费额度够用吗？**
A：完全够用。免费计划包含：
- 无限带宽
- 每月 10 万次 Worker 请求
- 全球 CDN 加速
- 个人工作台一天用不了几百次请求，绰绰有余 ✨
